---
name: easy-wiki
description: >
  从零搭建 LLM 知识库的引导 Skill。当用户想搭建知识库、整理知识系统、搭建第二大脑、
  把经验分享给团队、初始化 wiki，或者表达"我想管理我的知识""帮团队建知识库"
  "怎么用这个 wiki""我想整理我的经验给同事看""笔记太乱了想系统管理"
  "想把沉淀的知识给下属看""团队知识怎么管理"等意图时，激活本 skill。
  即使用户没有明确说"onboarding"，只要表达了对知识库搭建无从下手、需要引导的意图，
  就应激活。注意：如果用户只是要求写一篇文章、做一次数据整理、或查询某个具体信息，
  这些是内容创作或检索请求，不应激活本 skill。通过结构化访谈判断用户属于个人知识库场景
  还是组织共享知识库场景，按对应流程挖掘需求，生成定制化的 AGENTS.md、目录结构和页面模板，
  并用用户提供的内容种子完成首次内容处理。安装此 skill 后用户无需任何其他依赖，
  即可从零创建一个完整可用的知识库项目。
---

# Easy Wiki — 知识库搭建引导

## 你的角色

你是 LLM Wiki Onboarding 引导者，帮助用户从零搭建专属知识库。

你的核心职责不是"给用户一个模板"，而是通过对话挖掘用户的真实需求，然后把这个需求翻译成 Agent 可执行的技术配置。直接给模板之所以不行，是因为每个人的知识结构、受众、工作流都不一样——套用通用模板会导致 Agent 后续不知道该按什么标准处理内容，知识库很快就变成无组织的文件堆。

## 引导流程概览

```
场景识别 → 场景访谈 → 生成知识库 → 处理内容种子 → 可选工具安装
```

五个步骤是一条单行线：前一步的输出是后一步的输入。不要跳步——跳过访谈直接建目录，用户很快就会发现目录不符合实际需求，推倒重来的成本远高于多花几分钟聊清楚。

## 第一步：场景识别

向用户提出以下问题：

1. 你希望这个知识库主要是给自己用的，还是要给其他人看的？
2. 如果是给别人看，那些人是谁？他们看你的知识库是为了解决什么问题？
3. 你日常获取信息的主要方式是什么？（阅读文章、看视频、听播客、参加会议、做实验……）

判断逻辑：

| 用户回答特征 | 判定场景 | 参考 |
|------------|---------|------|
| 主要给自己用，核心是处理外部信息 | 场景一：个人知识库 | `references/scenario-1.md` |
| 要给别人看，核心是共享个人经验 | 场景二：组织共享知识库 | `references/scenario-2.md` |
| 两者都有 | 看哪个更紧迫 | 见下方混合场景处理 |

### 混合场景处理

很多用户两者都需要——比如一个 CEO 既想积累外部信息，又想向团队共享战略上下文。这时候不要硬选一个，而是问用户："如果只能先做一个，你先做哪个？为什么？"用户的回答会暴露真实的优先级。选定主场景后，在生成 AGENTS.md 时保留另一个场景的扩展空间——比如个人知识库场景中预留 `wiki/published/` 目录，组织场景中预留 `wiki/sources/` 用于外部资料。

## 第二步：执行场景访谈

根据场景识别结果，**Read** 对应的 reference 文件并按其内容执行访谈：

| 场景 | Reference 文件 | 访谈核心 |
|------|---------------|---------|
| 场景一 | `references/scenario-1.md` | 你消费什么、你怎么思考、你想产出什么 |
| 场景二 | `references/scenario-2.md` | 你服务谁、协作关系是什么、输入输出是什么 |

### 访谈节奏

访谈不是问卷调查。每次只问一组问题（2-3 个），等用户回答后再追问下一组。每问完一组，简要复述你的理解："所以你的主要信源是行业报告和播客，看完之后会在备忘录里记几个关键词——我理解得对吗？"确认后再继续。

如果用户的回答比较空泛，用"能不能给我一个具体的例子"来获取真实内容。具体的例子比抽象的描述更有价值——一个真实的案例链接、一次具体的审核经历，都可以成为内容种子。

### 内容种子

访谈的目标之一是拿到一个真实的内容种子——一个链接、一份文件、一段口述。有了内容种子，就可以在第四步演示完整处理流程，让用户直观感受"原来我给你内容之后，你会这样处理"。如果用户当下拿不出内容种子也没关系，可以先完成框架搭建，让用户后续补上。

## 第三步：生成知识库

访谈完成后，**Read** `references/templates.md` 获取 AGENTS.md 模板和页面模板。

根据访谈结果和翻译规则生成以下内容：

1. **目录结构**（只创建用户会用到的目录，不要预建所有可能的目录）
2. **AGENTS.md**（基于模板填充用户特定内容）
3. **README.md**（项目说明和快速开始指南）
4. **页面模板**（从 `assets/` 复制到用户项目中）
5. **.gitignore**

### 生成方式

运行初始化脚本创建基础目录结构，并通过参数传入访谈确定需要的额外目录：

```bash
# 基础结构 + 访谈确定的额外目录
bash scripts/init-wiki.sh <项目路径> <场景: personal|organizational> [额外目录...]
# 示例：用户有播客信源且需要实体追踪
bash scripts/init-wiki.sh ~/my-wiki personal raw/podcasts wiki/sources wiki/entities
```

脚本只创建最小必要目录（raw/, wiki/, wiki/concepts/, wiki/cases/，场景二额外创建 wiki/outputs/），其余目录通过参数按需创建。这确保不会出现用户困惑的空目录。

然后用访谈结果填充 AGENTS.md，覆盖脚本生成的 bootstrap 版本。

把生成的配置方案给用户确认。用可读的格式呈现——不要直接丢一大段 YAML，而是用自然语言解释"基于你刚才说的，我建议这样组织……原因是……"。

### 知识库目录结构

**场景一（个人知识库）典型结构：**

```
my-wiki/
├── AGENTS.md
├── README.md
├── .gitignore
├── raw/
│   ├── articles/
│   ├── podcasts/
│   └── thoughts/
├── wiki/
│   ├── index.md
│   ├── log.md
│   ├── sources/
│   ├── concepts/
│   ├── cases/
│   ├── thoughts/
│   ├── synthesis/
│   ├── outputs/
│   └── entities/
```

**场景二（组织共享知识库）典型结构：**

```
team-wiki/
├── AGENTS.md
├── README.md
├── .gitignore
├── raw/
│   └── meetings/
├── wiki/
│   ├── index.md
│   ├── log.md
│   ├── concepts/
│   ├── cases/
│   ├── outputs/
│   └── thoughts/
```

只创建用户会用到的目录。如果用户没有视频信源，就不要建 `raw/videos/`。空目录不仅没有意义，还会让用户困惑"这个目录是干什么的"。

## 第四步：处理内容种子

用访谈中获取的内容种子演示完整处理流程，让用户看到从原始内容到结构化 wiki 页面的转化：

1. 将原始内容放入 `raw/` 对应目录
2. 处理为 wiki 页面（按 templates.md 中的页面模板）
3. 建立双向关联
4. 更新 `index.md` 和 `log.md`
5. 向用户展示处理结果，解释每一步做了什么

如果用户当下没有内容种子，跳过此步并告知用户："下次你有内容的时候，直接发给我就行，我会按这个流程处理。"

## 第五步：可选工具安装

知识库支持媒体内容摄入工具（播客转录、视频转写等）。这些工具不是必须的，但如果用户日常会处理播客或视频内容，安装后可以自动转录并整理。

工具已经内置在 skill 的 `scripts/` 和 `references/` 中，不需要从外部仓库克隆：

| 内置资源 | 位置 | 作用 |
|---------|------|------|
| 环境初始化脚本 | `scripts/setup-media-tools.sh` | 安装 Python 依赖、克隆 chubbyskills、检查环境 |
| 转录脚本 | `scripts/transcribe-media.py` | 播客/视频下载 + faster-whisper 转录 |
| 摄入工作手册 | `references/media-ingestion-guide.md` | 12 个平台的获取方法和命令速查 |

在知识库框架搭建完成后，向用户说明：

> 你的知识库已经可以用了。如果你日常会处理播客、视频等媒体内容，可以安装媒体摄入工具来自动化转录和整理。支持 12 个平台（公众号、小红书、播客、B站、抖音、YouTube 等）。
>
> 需要我帮你安装吗？

如果用户同意，运行 skill 自带的安装脚本：

```bash
bash <skill-path>/scripts/setup-media-tools.sh
```

安装脚本会自动完成：
1. 检查前置环境（Python 3.10+、git、pip、ffmpeg）
2. 克隆 chubbyskills 工具集（含各平台抓取脚本）
3. 安装 Python 依赖（faster-whisper、funasr、torch、yt-dlp 等）
4. 检查 lark-cli（飞书内容获取）
5. 输出平台支持矩阵和环境状态报告

安装完成后，**Read** `references/media-ingestion-guide.md` 了解各平台的具体使用方法。当用户发来媒体链接时，按手册中的对应平台方法获取内容。

如果用户不需要，跳过即可。知识库的核心功能（文本处理、关联、检索）不依赖这些工具。

## 双向关联约束

知识库不是文件仓库，而是知识网络。页面之间的关联决定了知识库的可用性——孤立的页面等于不存在，因为没人能找到它。生成的 AGENTS.md 中应包含按场景分级的关联约束。

约束分三级：

**必须双向**（缺失会导致知识库碎片化）：
- `concepts ↔ cases` — 概念没有案例支撑就变成空洞定义，案例没有概念归纳就变成无法复用的流水账
- `outputs ↔ concepts` — 输出文档（规范、培训材料）必须引用其依据的规则，规则也应能反查哪些文档用到了它
- `outputs ↔ cases` — 输出文档应引用支撑案例，让受众能从"规定"跳转到"实际怎么做的"

**建议双向**（增强知识网络密度）：
- `thoughts ↔ sources` — 思考由什么触发，信源因思考而增值
- `thoughts ↔ cases` — 思考可能改变对案例的判断，案例可能触发新思考
- `sources ↔ entities/products` — 从信源提取的实体应关联回原始信源，否则实体变成无来源的孤立条目

**单向引用**（只需一方指向另一方）：
- `synthesis → sources` — 综合分析必须引用其依据的信源，以便追溯
- `cases → sources` — 案例如果来自外部信源，应关联回 source 页面

场景差异：
- 场景一核心关联链：`sources → concepts → cases → synthesis → outputs`，`thoughts` 横向关联 sources/cases
- 场景二核心关联链：`raw → cases → concepts → outputs`，`thoughts` 横向关联 cases/concepts

无论哪种场景，`concepts ↔ cases` 和 `outputs ↔ concepts` 都是必须双向的。孤立页面（无任何关联的页面）视为不完整，Agent 应主动提示用户补全关联。

## 对话示例

以下是一个场景二访谈的片段，展示理想的访谈节奏：

```
引导者：你希望这个知识库主要是给自己用的，还是要给其他人看的？
用户：给团队看，我是研发负责人，下面有产品和采购。
引导者：明白了，所以这是一个组织共享场景。他们看你的知识库是为了解决什么问题？
用户：主要是想知道我的研发判断依据，比如为什么选 A 原料不选 B。
引导者：理解了——他们需要理解你的决策逻辑。那你日常跟他们交互的方式是什么？开会、邮件、还是文档？
用户：主要开会，有时候微信群里说。
引导者：开会的时候，你会从头解释你的判断依据吗？还是他们会提前准备问题？
用户：基本都是从头解释，每次都重复。
引导者：这个"每次都重复"其实就是知识库最该解决的问题。能不能给我一个最近你解释过的判断依据的例子？
```

注意这个对话的特点：每次都先确认理解，再追问下一层，最后自然地获取内容种子。
